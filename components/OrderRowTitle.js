const OrderRowTitle = ({ title }) => {
    return (
        <h2 className="text-2xl font-bold underline underline-offset-2 decoration-2 decoration-cyan-700">
            {title}
        </h2>
    );
};

export default OrderRowTitle;
